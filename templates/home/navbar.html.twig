
<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <div class="navbar-brand p-0 m-0" href={{ path('app_home') }}>
      <img class="bg-white rounded-circle p-1" src="{{ asset('build/images/NUSAvolunteer/NUSAvolunteer_logo_final.svg') }}" alt="NUSAvolunteer logo" width="60" height="auto">
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav me-auto mb-2 mb-lg-0 w-100 d-flex justify-content-end align-items-center">
        <div class="nav-item">
          <a class="nav-link active nav-buttons" aria-current="page"
            {% if app.user %} 
              href="{{path('app_logout')}}">
              Logout
            {% else %}
              href="{{path('app_register')}}">
              Sign in
            {% endif %}
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>